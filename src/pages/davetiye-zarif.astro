---
import { site } from "@/i18n/content";
import HornDivider from "@/components/motifs/HornDivider.astro";
import CornerMotif from "@/components/motifs/CornerMotif.astro";
import Suzani from "@/components/motifs/Suzani.astro";

const title = "Ulya & Ali · Düğün Davetiyesi";
const description =
  "25 Temmuz 2026 · Güzelcehisar Garden, İstanbul. Düğünümüze davetlisiniz.";
---

<!doctype html>
<html lang="tr">
  <head>
    <script is:inline>
      document.documentElement.classList.add("js");
    </script>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,500;0,600;1,400;1,500&family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;1,300;1,400&display=swap"
      rel="stylesheet"
    />
    <meta name="theme-color" content="#f5f1e8" />
  </head>

  <body class="stage">
    <main class="wrap">
      <article class="card">
        <span class="frame" aria-hidden="true"></span>
        <span class="corner corner--tl" aria-hidden="true"><CornerMotif /></span>
        <span class="corner corner--tr" aria-hidden="true"><CornerMotif /></span>
        <span class="corner corner--br" aria-hidden="true"><CornerMotif /></span>
        <span class="corner corner--bl" aria-hidden="true"><CornerMotif /></span>

        <div class="inner">
          <!-- top group -->
          <div class="top">
            <span class="medallion reveal"><Suzani /></span>
            <p class="eyebrow reveal" style="animation-delay:0.14s">Evleniyoruz</p>
          </div>

          <!-- names: the commanding moment -->
          <h1 class="names reveal" style="animation-delay:0.26s">
            <span class="names__row">Ulya</span>
            <span class="names__amp">&amp;</span>
            <span class="names__row">Ali</span>
          </h1>

          <div class="divider reveal" style="animation-delay:0.4s">
            <HornDivider class="divider__art" />
          </div>

          <p class="invite reveal" style="animation-delay:0.5s">
            En özel günümüzde, sizi de aramızda görmekten mutluluk duyarız.
          </p>

          <!-- children note, above the date -->
          <p class="note reveal" style="animation-delay:0.6s">
            Minik misafirlerimiz, sizleri çok sevsek de bu akşam tatlı
            rüyalarda olmanızı diliyoruz. İyi geceler.
          </p>

          <!-- details -->
          <div class="meta reveal" style="animation-delay:0.68s">
            <p class="meta__date">25 Temmuz 2026</p>
            <p class="meta__sub">Cumartesi &nbsp;·&nbsp; 19.00</p>
            <p class="meta__venue">{site.venueName}</p>
            <p class="meta__addr">Otağtepe, Kavacık &nbsp;·&nbsp; Beykoz / İstanbul</p>
          </div>
        </div>
      </article>

      <p class="hint">Yazdırmak veya PDF için Ctrl/Cmd + P</p>
    </main>
  </body>

  <style>
    :root {
      --paper: #f6f2e9;
      --ink: #2a2722;
      --ink-soft: #837a6d;
      --gold: #a8854c;
      --gold-soft: #c3a574;
      --frame-inset: clamp(0.85rem, 2.6vw, 1.25rem);
      --display: "Playfair Display", Georgia, serif;
      --serif: "Cormorant Garamond", "Times New Roman", serif;
    }

    .stage {
      margin: 0;
      min-height: 100dvh;
      background: #e7e1d4;
      display: grid;
      place-items: center;
      padding: clamp(1rem, 4vw, 3rem) 1rem;
    }
    .wrap { display: flex; flex-direction: column; align-items: center; gap: 1.1rem; }

    .card {
      position: relative;
      width: min(94vw, 520px);
      min-height: min(150vh, 748px);
      display: flex;
      background:
        radial-gradient(120% 80% at 50% 0%, #faf7ef, var(--paper) 60%);
      box-shadow: 0 34px 80px -34px rgba(60, 52, 38, 0.5);
    }

    .frame {
      position: absolute;
      inset: var(--frame-inset);
      border: 1px solid color-mix(in srgb, var(--gold) 45%, transparent);
      pointer-events: none;
    }

    .corner {
      position: absolute;
      width: clamp(26px, 6.4vw, 38px);
      line-height: 0;
      color: color-mix(in srgb, var(--gold) 88%, transparent);
      pointer-events: none;
    }
    .corner :global(svg) { width: 100%; height: auto; display: block; }
    .corner--tl { top: var(--frame-inset); left: var(--frame-inset); }
    .corner--tr { top: var(--frame-inset); right: var(--frame-inset); transform: rotate(90deg); }
    .corner--br { bottom: var(--frame-inset); right: var(--frame-inset); transform: rotate(180deg); }
    .corner--bl { bottom: var(--frame-inset); left: var(--frame-inset); transform: rotate(270deg); }

    .inner {
      flex: 1;
      width: 100%;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 0.72rem;
      text-align: center;
      padding: 2.1rem 2.7rem;
    }

    .top { display: flex; flex-direction: column; align-items: center; gap: 0.55rem; }
    .medallion { width: 44px; line-height: 0; color: var(--gold); }
    .medallion :global(svg) { width: 100%; height: auto; display: block; }

    .eyebrow {
      font-family: var(--serif);
      font-style: italic;
      font-weight: 400;
      letter-spacing: 0.46em;
      text-transform: uppercase;
      font-size: 0.74rem;
      color: var(--gold);
      margin: 0;
      padding-left: 0.46em;
    }

    .names {
      font-family: var(--display);
      font-weight: 400;
      color: var(--ink);
      line-height: 0.96;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 0.04em;
      margin: 0;
    }
    .names__row { font-size: 3.1rem; letter-spacing: 0.005em; }
    .names__amp {
      font-family: var(--serif);
      font-style: italic;
      font-weight: 400;
      color: var(--gold);
      font-size: 1.7rem;
      line-height: 1;
      margin: 0.04em 0;
    }

    .divider { display: flex; justify-content: center; color: var(--gold); }
    .divider :global(svg) { width: clamp(150px, 42%, 215px); height: auto; }

    .invite {
      font-family: var(--serif);
      font-weight: 400;
      color: var(--ink-soft);
      max-width: 26ch;
      font-size: 1.12rem;
      line-height: 1.55;
      margin: 0;
      text-wrap: balance;
    }

    .meta { display: flex; flex-direction: column; align-items: center; gap: 0.18rem; }
    .meta__date {
      font-family: var(--display);
      font-weight: 500;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      font-size: 1.22rem;
      color: var(--ink);
      margin: 0;
    }
    .meta__sub {
      font-family: var(--serif);
      letter-spacing: 0.34em;
      text-transform: uppercase;
      font-size: 0.74rem;
      color: var(--gold);
      margin: 0.1rem 0 0.7rem;
      padding-left: 0.34em;
    }
    .meta__venue {
      font-family: var(--serif);
      font-weight: 600;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      font-size: 0.92rem;
      color: var(--ink);
      margin: 0;
      padding-left: 0.2em;
    }
    .meta__addr {
      font-family: var(--serif);
      letter-spacing: 0.06em;
      font-size: 0.86rem;
      color: var(--ink-soft);
      margin: 0.22rem 0 0;
    }

    .note {
      font-family: var(--serif);
      font-style: italic;
      font-size: 0.84rem;
      line-height: 1.5;
      max-width: 32ch;
      color: var(--ink-soft);
      margin: 0;
    }

    .hint { font-family: var(--serif); font-size: 0.88rem; letter-spacing: 0.04em; color: #9b9384; }

    .reveal { opacity: 0; transform: translateY(12px); animation: rise 0.9s cubic-bezier(0.22, 1, 0.36, 1) forwards; }
    @keyframes rise { to { opacity: 1; transform: translateY(0); } }

    @page { size: A4 portrait; margin: 0; }
    @media print {
      .stage { background: #fff; padding: 0; display: block; }
      .hint { display: none; }
      .card { width: 100%; min-height: 0; height: 100vh; box-shadow: none; }
      :global(html), :global(body) { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
    }
    @media (prefers-reduced-motion: reduce) { .reveal { animation: none; opacity: 1; transform: none; } }
  </style>
</html>
